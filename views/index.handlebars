<h1>Mongo Scraper</h1>

<button id="scrapeBtn">SCRAPE!</button>

<br>

{{#each results}}

{{inc @index}})
<br>
    <p>
        Title: {{title}}
    <br>
        Link: <a target="_blank" href="{{URL}}">{{URL}}</a>
    <br> 
        Summary: {{summary}}
    <br>
        Image: <img src="{{image}}">
    <hr>
    </p>


{{/each}}

